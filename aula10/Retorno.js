function CalculaArea(altura,largura){
    const CalculaArea = altura * largura
    return CalculaArea 
}

// Atribui um retorno á uma variavel 
const AreaCalculada = (CalculaArea(2,3))

// Imprime retorno no console
console.log(CalculaArea + 3)


